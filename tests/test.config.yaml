generation:
  provider: nlp # tgis or nlp
  service:
    hostname: localhost
    port: 443
chunkers:
  test_chunker:
    type: sentence
    service:
      hostname: localhost
      port: 8085
detectors:
  test_detector:
    type: text_contents
    service:
      hostname: localhost
      port: 8000
    chunker_id: test_chunker
    default_threshold: 0.5
  content_detector:
    type: text_contents
    service:
      hostname: 0.0.0.0
      port: 8001
    chunker_id: whole_doc_chunker
    default_threshold: 0.5
